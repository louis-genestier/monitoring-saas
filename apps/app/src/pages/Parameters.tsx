import { Button } from "@repo/ui";

export const Parameters = () => {
  return (
    <div>
      <h1>Parameters</h1>
      <p>Change your settings here.</p>
      <Button onClick={async () => {}}>Test lol</Button>
    </div>
  );
};
